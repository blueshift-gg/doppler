{
  "name": "doppler-workspace",
  "private": true,
  "type": "module",
  "workspaces": ["packages/*", "examples"],
  "scripts": {
    "build": "tsc -b packages/kit packages/web3js",
    "test": "tsc -p tsconfig.test.json && bun test"
  },
  "devDependencies": {
    "@types/bun": "^1.2.0",
    "typescript": "^5.9.0"
  }
}
